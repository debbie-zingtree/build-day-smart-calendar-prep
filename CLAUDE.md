# CLAUDE.md

## Project Overview
[One sentence: What does this project do?]

[Two or three sentences: Who is it for, and what problem does it solve?]

## Tech Stack
- **Language**: [e.g., TypeScript]
- **Framework**: [e.g., Next.js / React / none]
- **Database**: [e.g., Postgres / SQLite / none]
- **Deployment**: [e.g., Vercel / Netlify / static host]

## Coding Style
- Use ES modules (`import/export`), never CommonJS (`require`).
- Prefer `async/await` over `.then()` chains.
- Use 2-space indentation.
- Descriptive variable names — no single letters except loop counters.
- Comment the *why*, not the *what*.

## Project Rules for Claude
- **Critical files (ask before modifying)**: [e.g., `src/db/schema.sql`, build config, `package.json`]
- [Add any project-specific rule, e.g., "No new dependencies without approval."]
- [Add any "do not" rule, e.g., "Do not add a backend without asking."]

## Before Any Feature Work — Read These First
- **`PRD.md`** — WHAT and WHY. Confirm the goal and the "done" checklist.
- **`spec/plan.md`** — HOW. Follow the build order unless you have a reason to deviate (and say so).
- **`SECURITY.md`** — read before touching secrets, auth, user input, or external calls.

## What Claude Should Know
- [Anything non-obvious about how this project is built or run.]

## Out of Scope
- [List features or directions Claude should NOT pursue for v1.]
